# Copy this file to .env and fill in real values locally.
# .env is gitignored — never commit real secrets.

# Example: API key for a future assignment that calls an external service
API_KEY=

# Example: path override for the data directory
DATA_DIR=
